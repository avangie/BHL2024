services:
   front:
     build:
       context: ./front
     environment:
       NODE_ENV: production
     ports:
       - 3000:3000
     depends_on:
       - backend

   backend:
     build:
       context: ./backend
     ports:
       - 8000:8000
